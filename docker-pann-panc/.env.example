# Optional Docker Compose bind mount overrides.
#
# Copy this file to .env or set the variables in PowerShell before running
# docker-compose. TRAIN_DATA should point at a class-folder dataset.

TRAIN_DATA=../pann-panc/data
MODELS_DIR=../pann-panc/models
REPORTS_DIR=../pann-panc/reports
